[pytest]
testpaths = sample_project/tests
pythonpath = .
addopts = -v --tb=short --strict-markers
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    security: marks tests that verify security hardening
    coverage: marks tests critical for coverage
